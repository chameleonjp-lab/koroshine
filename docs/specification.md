# コロシャイン 仕様書

## ゲーム概要

- タイトル: コロシャイン
- 説明: くるくる回して落ち続けろ。🌰を集めて高得点を狙え。
- 公開予定URL: https://chameleonjp.codeberg.page/koroshine/
- 形式: スマホ向けブラウザゲーム。`index.html` 単体公開可能構成。
- 参考: 既存ゲームの仕組みのみ参考にし、コピーはしない。

## ゲームルール

- 太陽（球）は**重力で落下し、床に当たると跳ね返る**（物理演算: 重力加速・速度積分・反発）。
- プレイヤーは左右ドラッグのみで塔を回転させる。
- 各階の床は中実のスライス（`SEG=8` 分割）で構成され、各スライスは「穴（すき間）」「安全帯」「危険帯」のいずれか。正面が穴なら通過、安全帯なら跳ね返り、危険帯なら終了。
- 穴を通すと下階へ落下する。跳ねずに連続で落ちるとコンボになる。
- **3連続落下でファイアボール化**し、続く6階ぶん（`FIRE_FLOORS=6`）のスライス（危険帯含む）を破壊して貫通落下する。6階突破後は通常状態へ戻るが、ファイア終了直後の次の接触1回だけ、危険帯即死を防ぐ着地猶予がある。
- スライスの色自体が状態を表す（安全帯＝ゾーン配色 / 危険帯＝赤 `#e8112d`（全ゾーン共通） / 穴＝空白）。現在階の接触スライスはわずかに明るく強調する。判定値はこの表示と一致する。
- 通常時に危険帯へ乗るとゲーム終了。ファイアボール中は破壊して突破する。ファイア終了直後の着地猶予で危険帯に触れた場合は、安全着地扱いで跳ね返り、スコア加算も階層進行も行わず、その後は通常判定に戻る。
- 🌰を取得するとスコアを得る。
- 🌰取得演出では、取得対象が `🌰` から `☀️` に変化し、`+50` を表示する。

## 物理演算

- 重力加速度 `GRAV` で鉛直速度 `bvy` を積分し、位置 `by` を更新する。`bvy` は上限 `MAXV` でクランプする。
- 安全帯に着地した場合は反発初速 `BOUNCEV` で上方へ跳ね返し、コンボをリセットする。
- 穴を正面に合わせた場合は反発せず落下を継続し、`+100`・コンボ・一気落下判定を行う。
- ファイアボール中は落下速度を `FIREV` 以上に保ち、安全帯・危険帯のスライスを穴へ変えて（破壊して）通過する。**6階（`FIRE_FLOORS`）突破した時点で通常状態へ戻り、コンボもリセットして連鎖再点火を防ぐ**（時間では消えない）。6階突破後に次の床が残っている場合は `postFireGrace=1` をセットし、次の接触1回だけ危険帯即死を防ぐ。
- フレーム変動やiOSの低フレームでも一定挙動とするため、固定タイムステップ `1/120` 秒＋アキュムレータで物理を進め、トンネリングを防ぐ。
- 当たり判定は正面角度 `front = normAngle(π/2 - 回転角)` のスライス種別（穴/危険/安全）で行い、描画の正面と完全一致させる。
- 回転は指の移動ピクセル×固定係数を角度へ直接加算（離した後は軽い慣性）。接触は球の幅（中心・左右端の3点）で判定する。コンボ等の計時はゲーム内時間 `inGameTime` を用いる。詳細は「v1.1」「v1.3」節を参照。

## 勝利条件

- 40階到達で勝利。
- 40階到達とは、39階の穴を通過して表示上の現在階層が40階になった時点を指す。
- 40階到達時に `+3000点` を加算する。

## 敗北条件

- 危険床（危険帯）への接触で敗北。ただしファイアボール中は破壊して突破する。ファイア終了直後の着地猶予が残っている場合のみ、次の接触1回で危険帯即死を防ぎ、スコア加算なし・階層進行なしで跳ね返す。
- 危険床接触時は即座に結果画面へ移らず、約0.38秒だけ失敗箇所を表示してから結果画面へ進む。待機中はスコア加算、当たり判定、ランキング通信を行わない。
- リタイア操作で任意終了。誤操作防止のため、リタイアは確認ダイアログでの了承後に終了する。
- リタイア時も結果画面へ進み、その時点のスコアをランキング送信する。ただし40階到達扱いにはしない。

## スコア

| 条件 | 加点 |
| --- | ---: |
| 1階突破 | +100 |
| 🌰取得 | +50 |
| 2連続落下コンボ | +50 |
| 3連続落下コンボ | +100 |
| 4連続落下コンボ | +150 |
| 5連続以降 | 1段ごとに +50 ずつ増加 |
| 3階以上一気落下（ファイアボール化） | +300 |
| ファイアボール中の各階通過 | +50 |
| 40階到達 | +3000 |

- 3階以上一気落下ボーナスは、850ms以内の穴通過が3回連続したときに `+300` し、ファイアボール（`FIRE_FLOORS=6` 階）を発動する。
- ファイアボール中は各階通過ごとに `+50`（床破壊・穴通過いずれも同額）。1発の発動で正確に6階を貫通し、終了時にコンボをリセットする。ファイア終了直後の着地猶予による跳ね返りではスコアを加算しない。
- プレイ中は2連続以上でコンボ表示を行い、3連続以上では一気落下チャンスを表示する。
- 一気落下予告として、`dropBurst` が1なら `連続落下 1/3`、2なら `あと1回で一気落下` を表示し、850ms以上空いたら消す。
- 連続状態が続く場合も、一気落下ボーナスは3回ごとに1回まで発生する。
- 穴通過の間隔が850ms以上空いたら、一気落下カウントをリセットする。
- ランキングは高いスコアが上位。並び順は `desc`。
- `RESULT` 中はスコア加算を停止する。

## ランキング

- 本番Supabaseランキング連携を使用する。
- Supabase URL: `https://mlpnjgezrnhdxsxolyzj.supabase.co`
- Publishable key: 公開HTMLに配置可能な `sb_publishable_...` キーを使用する。
- secret key / service_role key は使用禁止。
- `public.scores` は使用禁止。
- 利用対象は `public.games` / `public.game_scores` / 既存RPC。
- スコア送信RPCは `submit_score`。
- ランキング取得RPCは `get_best_score_ranking`。
- プレイ統計RPCは必要に応じて `get_game_play_stats`。
- `game_slug` は `koroshine` 固定。
- 送信値は `p_display_name` / `p_game_slug` / `p_score` / `p_client_version`。
- スコア種別はベストスコア型で、高い方が良い。
- `score_order`: `desc`、`score_unit`: `点`、`score_scale`: `1`、`score_decimals`: `0`、`top_ranking_type`: `best`。
- ゲーム終了時に自動送信する。クリア、危険床接触、リタイアのいずれも対象。
- 送信は1プレイにつき1回のみ。
- ランキング取得も結果画面表示時に1回のみ。
- 結果画面では結果を先に表示し、その後に送信中、成功または失敗、ランキング取得結果を表示する。
- 送信失敗時も結果表示を継続し、画面には `ランキング送信に失敗しました。スコアはこの画面で確認できます。` と短く表示する。
- 取得失敗時は `ランキング取得に失敗しました。時間をおいて再度お試しください。` と短く表示する。
- 詳細エラーは画面に長く表示せず、開発確認用に `console.warn` へ出す。
- ローカルランキングを本番ランキング欄の代替として混同表示しない。
- 結果画面にランキング登録ボタンは置かない。

## 状態管理

必須状態は以下。

- `HOME`
- `RULE`
- `NAME`
- `READY`
- `PLAYING`
- `RESULT`
- `ERROR`

状態別の制約:

- `PLAYING` のみゲーム更新・当たり判定・スコア加算を行う。
- `RESULT` 中はスコア加算、当たり判定、ゲーム更新を停止する。
- `ERROR` は致命的な初期化失敗などの表示に使う。

## 画面構成

### ホーム

- タイトル
- ゲーム説明
- ゲーム開始
- ルール説明
- ゲームをシェア
  - シェア本文は `コロシャイン`、説明文、`PUBLIC_URL` の3行で構成し、本文末尾に必ず `PUBLIC_URL` を含める。
  - Web Share API使用時も `url` フィールドには依存せず、`text` フィールド内の末尾URLを共有する。
- その他のゲーム

### 名前入力

- ランキング利用のため必須。
- 10文字程度。
- `localStorage` 保存可。

### ゲーム画面

- 上部: スコア、現在階層、リタイア
- 中央: Canvas
- 下部: 簡易説明

### プレイ補助表示

- スライスの色で状態を示す（安全帯＝ゾーン配色 / 危険帯＝赤（全ゾーン共通） / 穴＝空白）。現在階の接触スライスはわずかに明るく強調する。
- 太陽の落下/反発/取得/失敗に粒子・接地影・残像・スクワッシュ・着地スプラッシュ・画面シェイク・触覚振動でフィードバックする。
- 2連続以上の穴通過でコンボ状態を、ファイアボール中は「🔥 ファイアボール！」をCanvas上に表示する。
- 6階ごとにゾーン配色が変化し、背景を連続補間する。
- `dropBurst` が1なら `連続落下 1/3`、2なら `あと1回で一気落下` をCanvas上に表示する。
- 危険床接触時は約0.35秒、失敗箇所と短い説明をCanvas上に表示する。

### 結果画面

- 結果
- 到達階
- スコア
- 到達層だけに応じた結果コメント（終了理由欄は別表示）
- ランキング送信状態
- ランキング
- もう一度遊ぶ
- 結果をシェア
  - シェア本文は `コロシャイン`、現在のスコア、到達階、`PUBLIC_URL` の4行で構成し、本文末尾に必ず `PUBLIC_URL` を含める。
  - Web Share API使用時も `url` フィールドには依存せず、`text` フィールド内の末尾URLを共有する。
- その他のゲーム

## スマホ対応

- iPhone中心。
- iPhone SE幅に対応する。
- 操作は左右ドラッグのみを優先する。
- 以下を禁止・抑制する。
  - ピンチズーム
  - ダブルタップ拡大
  - 長押し選択
  - テキスト選択
  - 横スクロール

## 必須演出

- 落下・反発時の振動と粒子（着地ダスト）演出。
- 太陽のスクワッシュ＆ストレッチ、影、残像。
- コンボ演出。
- 🌰取得時の `🌰 → ☀️` 演出と `+50` 表示、取得粒子。
- 危険帯接触演出（失敗位置リング・粒子・画面シェイク）。
- ファイアボール演出（発火・炎の粒子・加算グロー・破壊スマッシュ・加点）。
- 着地スプラッシュ痕とゾーン配色変化。
- スライスの色による状態提示と接触スライスの強調。
- コンボ状態表示と一気落下予告。
- 結果画面のプレイ評価コメント。
- クリア演出。
- タップやドラッグ時の即時反応。

## 性能予算

- 目標: 60fps。
- 最低: 30fps維持。
- 表示床: 最大8階。
- アイテム: 最大8個。
- エフェクト: 最大60個。
- 描画: 最大100要素程度。
- DOM更新: 最小限。毎フレームのDOM生成・重いオブジェクト生成を行わない。
- ランキング通信: 送信1回、取得1回。

## v1.1 操作感・接触感・立体感 改善

ゲーム内容（スコア式・ランキング・画面遷移・30階クリア・ファイアボール・単一HTML・外部依存ゼロ）は変えず、操作感と表現のみ改善した。本v1.1は Codex 実装（PR #10）と本実装を統合したもの。慣性・通過/失敗演出は Codex 実装を採用し、通過判定（依頼書準拠の球幅）と疑似3D奥行き（Phase 5）を本実装で上乗せしている。

### Phase 1: 回転操作に「重さ」（慣性）— Codex実装を採用
- 追加定数: `MAX_ANG_V=8.5`（最大角速度・rad/秒）/ `ANG_DRAG=15`（減衰係数）。
- 追加状態: `game.angularVelocity`（rad/秒）、`game.input.lastT`。
- 変更関数: `pointermove`（指の移動量と経過時間から目標角速度 `clamp(dx*SENS/dt, ±MAX_ANG_V)` を求め、`av=av*0.45+target*0.55` で追従）、`pointerdown`（掴んだ瞬間 `av*=0.35` で減衰）、`stepAnims(dt)`（`angle+=av*dt`、`av*=max(0,1-ANG_DRAG*dt)` で減速、`|av|<0.01` で停止）。秒基準のため fps 差に強い。
- 挙動: 指の速度に追従して回り、離すと慣性で少し回って止まる。
- 変更しない: `SENS`、スコア計算。

### Phase 2: 接触判定に「球の幅」を反映（通過判定は依頼書準拠に調整）
- 追加関数: `collisionAt(p)` → `{front,span,center,left,right,type,result,wi}`。`wedgeIndexAt(a)` は角度引数対応で残す。
- 角度半幅: `span = min(WS*0.44, asin(min(0.95, RBALL/R))*1.15)`。正面の中心・左端・右端のくさびを見る。
- 判定（依頼書準拠）: **中心・左端・右端すべてが穴なら pass**、いずれかが危険帯なら danger、中心が穴でも端が床なら edge（=跳ね）、それ以外は bounce。ファイアボール中は pass 以外を破壊（`smash`）。
- 接続関数: `stepPhysics()` を `collisionAt(p)` 経由に変更。
- `?debug` 時のみ `result/center/left/right/wi` と角度・span を画面下部に表示（`game.debugHit`・`drawHints`）。通常プレイは非表示。
- 挙動: 穴の中央のみ安定通過、端は跳ね、危険帯に少しでも触れたら失敗。見た目と判定のズレを低減。

### Phase 3: 床通過の瞬間の演出 — Codex実装を採用
- 追加状態: `game.passFX = {x,y,life,max,zone,gStart,gW,angle}`。
- 変更関数: `passFloor()`（通過時に `passFX` 起動・`squash=-1` で縦伸び・小粒子）、`stepAnims()`（`passFX.life` 減衰・通過中はカメラ追従を `dt*20` に加速・負の `squash` を戻す）、新規 `drawPassFX()`（穴の縁を発光）、`drawBall()`（`squash<0` で縦に伸びる）。
- 変更しない: スコア・階層進行・クリア判定。

### Phase 4: 危険床の失敗演出強化 — Codex実装を採用
- 追加状態: `game.failHit`（`collisionAt` の結果）。
- 変更関数: `die()`（接触した危険くさび `failHit.wi` を保存、`squash=1.15`）、`drawPlatform()`（`pendingFinish` 中は接触した危険くさびを赤くハイライト＋縁取り）。
- 変更しない: 結果理由「危険床接触」、ランキング送信1回。

### Phase 5: 疑似3Dの奥行き強化（Canvas 2Dのまま）— 本実装
- 変更関数: `drawPlatform()`。各くさびの画面前後 `sin((i+0.5)*WS+phi)` で手前を明るく（白を最大10%）・奥を暗く（黒を最大12%）する薄いオーバーレイを1枚追加。外周の影は既存の側面押し出し（暗色）で表現。
- 変更しない: ゾーン配色設計、危険帯と安全床のコントラスト。

### 検収（iPhone SE 320/375・iOS UA・実ブラウザ自動プレイ）
- 慣性: ドラッグで角速度生成（約2.5 rad/秒）→離すとコースト→停止。
- 球幅判定: 穴の中央=通過（result=pass）/ 穴の端=跳ね返り（result=edge, 同階に留まる）/ 危険帯接触=失敗。
- 通過演出: `passFX` 起動、縁の発光。
- 危険演出: 接触した危険くさびを赤くハイライト、理由「危険床接触」。
- 立体感: 手前/奥の明暗差。危険帯の視認性維持。
- 回帰なし: 30階クリア・ファイア中の危険帯貫通・本番でデバッグ非公開・JSエラー0・送信1/取得1。

## v1.3 ゲーム内時間・スワイプ安定化・ステージ生成のルール化

### 実時間依存の排除（タブ切替でコンボが消えない）
- 追加状態: `game.inGameTime`（秒。`stepPhysics` で `+=dt` 加算）。
- 変更: コンボ継続判定・一気落下のリセット・危険床待機（`pendingFinish.at`）の時間基準を `performance.now()`（実時間）から `game.inGameTime`（ゲーム内時間）へ置換。コンボ継続は `0.85` 秒、危険床待機は `0.4` 秒。
- 効果: バックグラウンド（タブ切替・着信）で `requestAnimationFrame` が止まっても、復帰時に `dt` をクランプして進むためコンボや進行が不自然に失われない。`startLoop` のフレーム基準（`last`）のみ実時間を使う（フレーム差分の算出にのみ必要）。

### スワイプ感度の安定化（端末非依存・指に吸い付く）
- 変更: `pointermove` を「フレーム間 `dt` での割り算による角速度算出」から、**指の移動ピクセル数 `dx × SENS` を角度へ直接加算**する方式へ。`dt` 依存を排除し、どの端末・フレームレートでも同じ指移動量で同じ回転量になる。
- 離した後は軽い慣性（`lastDragStep` をフレームごとに `×0.86` 減衰、`MAX_STEP` で上限）。`MAX_ANG_V/ANG_DRAG` と `angularVelocity` は廃止。

### ステージ生成のルール化（`makeStage(seed)`）
- プレイごとに `game.seed`（`reset` で乱数）を設定し、同じ階でも毎回異なるレイアウトにする（`rng(seed + n*K)`）。
- ルール1（直前の穴との角度ズレ）: 1〜10階は直前の穴の `±1` ブロック以内に配置して連続落下を誘発・操作学習、11階以降は真裏付近（`+SEG/2 ±1`）へ大きくズラして素早い回転を要求。
- ルール2（危険帯の確定配置）: まず穴のすぐ隣（ミスしやすい箇所）を確定で危険にし、残りはシードで散らす。穴の両隣が両方危険になった場合は片方を安全へ戻し、必ず通過できる肩を残す。
- ルール3（穴幅の段階的縮小）: 〜4階は2マス、5〜19階は乱数で1/2マス、**20階以降は必ず1マス**で終盤の緊張感を作る。
- 不変: 危険帯数の深度カーブ（4/10/18/26階で増加）、アイテム配置（`n%3===0`）、当たり判定・スコア。

### 検収（iPhone SE 320/375・iOS UA・実ブラウザ自動プレイ）
- スワイプ: 同じ120pxを高速/低速でドラッグしても回転量が同じ（実測ともに約1.5rad＝`120×SENS`）。
- ゲーム内時間: `inGameTime` が実時間相当で進み、`lastDropAt` がゲーム秒（実時間msではない）。
- 生成: 20階以降は穴1マス、序盤±1・中盤真裏、危険が穴の隣に確定、全階通過可能、プレイごとにレイアウトが変化。
- 生成: 20階以降は穴1マス、序盤±1・中盤真裏、危険が穴の隣に確定、全階通過可能、プレイごとにレイアウトが変化。
- 回帰なし: 30階クリア・ファイア中の危険帯貫通・通常時の危険帯終了・本番でデバッグ非公開・JSエラー0・送信1/取得1。

## v1.4 画面フィット・スワイプ速度・誤バック防止

### スワイプ速度ゲイン（速いほど大きく回る）
- `pointermove` で `gain = 1 + min(SWIPE_GAIN_MAX, |dx|*SWIPE_GAIN_K)` を掛け、`angle += dx*SENS*gain`。速い（1イベントの移動量が大きい）ほど大きく回転。
- 安定性: `dt` 割り算は使わず移動ピクセル量でゲインを出す（フレームレートで暴れない）。1イベントの回転は `STEP_CAP` で上限（瞬間移動・誤検知を防止）。離した後の慣性 `lastDragStep` はゲイン後の実回転量を引き継ぐ。
- 定数: `SWIPE_GAIN_K=0.03 / SWIPE_GAIN_MAX=2.2 / STEP_CAP=1.2`。

### 到達可能性のための生成リバランス
- 中盤（11階〜）の穴オフセットを「真裏（`SEG/2±1`）」から **2〜3ブロックの中程度ズレ（方向もシード）** に緩和。落下までに回しきれず詰む配置を解消（速いフリックで届く範囲）。
- 危険帯の上限を **4→3**、増加開始も後ろ倒し（`n>=4:1 / n>=12:2 / n>=22:3`）。深層が一面赤で潰れて安全な着地が無くなる問題を緩和。
- 「穴の両隣が両方危険なら片方を安全へ戻す」詰み防止と、穴中央は球幅判定で必ず通過可能、は維持。

### ビューポート固定・端スワイプ誤バック抑止
- プレイ中のみ `<html>` に `playing` クラスを付与。CSSで `body` を `position:fixed; inset:0; overflow:hidden; overscroll-behavior:none; touch-action:none` にし**ページを完全固定**（アドレスバー伸縮によるズレ・スクロールを排除）。左右パディングを0にして**全幅化**し、画面端まで `touch-action:none` 領域で覆う。
- プレイ中のみ `document` の `touchmove` を `preventDefault`（`passive:false`）し、ラバーバンド/パン/左端スワイプ（誤ブラウザバック）を抑止。結果画面のスクロールは阻害しない。
- `setState` の PLAYING 遷移で `playing` を付与、その他で除去。

### 検収（iPhone相当・実ブラウザ自動プレイ）
- スワイプ: 同じ120pxでも高速の回転量が低速の1.3倍超（速いほど大きく回る）。
- 生成: 中盤オフセットは2〜3ブロック、危険帯は最大3、全階通過可能、20階以降は穴1マス。
- 画面固定: プレイ中は `html.playing`＋`body{position:fixed}`、終了で解除。
- 回帰なし: 30階クリア・ファイア6階・危険床赤・本番でデバッグ非公開・JSエラー0・送信1/取得1。

## v1.5 マップを元に戻して40階へ拡張

- v1.4で到達性のために緩めていた生成（中盤オフセット・危険帯上限）を**元の内容へ戻す**。速度ゲイン（v1.4）で遠い穴も素早いフリックで届くため、元の難度でもクリア可能との判断。
  - 中盤（11階〜）の穴オフセットを再び**真裏付近（`SEG/2±1`）**に。
  - 危険帯の上限を再び**4**に（`n>=4:1 / n>=10:2 / n>=18:3 / n>=26:4`）。
- **`MAX_FLOOR` を 30→40** に拡張。クリア条件・HUD表示・スコア表示・シェア文・結果コメントを40階基準に更新（`+3000` のクリアボーナスは据え置き）。生成ルール（シード・序盤±1・穴の隣の危険・20階以降1マス）はそのまま40階へ適用。
- v1.1〜v1.4の他の変更（ゲーム内時間・スワイプ速度ゲイン・画面固定/誤バック防止・危険床赤・ファイア6階・疑似3D等）は維持。
- 検収（iPhone相当・実ブラウザ自動プレイ）: 穴中央整列で**40階クリア到達**、全40階が通過可能、中盤オフセットは真裏（距離3〜4）、危険帯は最大4、20階以降は穴1マス、画面固定・速度ゲイン維持、JSエラー0・送信1/取得1。

## 更新履歴
### v1.5
- v1.4で緩めた生成を元のマップに戻す（中盤は真裏付近、危険帯上限4）。速度ゲインで到達可能
- `MAX_FLOOR` を 30→40 に拡張（クリア/HUD/スコア/シェア/結果コメントを40階基準に）

### v1.4
- スワイプ速度ゲイン（速いほど大きく回る）を追加（`dt`非依存で安定）
- 中盤の穴オフセットを緩和し危険帯上限を3に下げて、深層の到達不可能/一面赤を解消
- プレイ中のページ完全固定＋全幅化＋`touchmove`抑止で、画面の揺れと左端スワイプの誤ブラウザバックを防止

### v1.3
- 実時間依存（`performance.now()`）をゲーム内時間（`inGameTime`）へ置換し、タブ切替でのコンボ消失を解消
- スワイプを「指の移動ピクセル×固定係数」の直接マッピングへ変更し、端末・フレームレート差による感度のブレを解消（離した後は軽い慣性）
- ステージ生成をシード＋階層ルール（直前の穴との角度ズレ／危険帯の確定配置／穴幅の段階縮小）でプロシージャル化

### v1.2
- 危険床の色を全ゾーン共通の赤（`#e8112d`）に統一（後半でも識別しやすく）
- ファイアボールを時間制から階数制へ変更：1発で正確に6階（`FIRE_FLOORS=6`）突破し、終了時にコンボをリセットして連鎖再点火を防止
- HUDのファイアボール表示に残り階数を追加

### v1.1
- 回転操作に慣性を追加
- 球の幅を考慮した接触判定を追加
- 床通過時の演出を追加
- 危険床接触時の表示を強化
- Canvas 2Dのまま奥行き表現を強化

## 結果コメント仕様（到達層ベース）

- 結果画面のコメントは、到達層だけで決まる。
- リタイア、危険床接触、クリアなどの終了理由はコメント選択に使わない。
- 終了理由欄はこれまでどおり別に表示し、コメントだけを到達層ベースにする。

| 到達層 | 結果コメント |
| --- | --- |
| 1〜5層 | いやぁ…なんていうか…お疲れ様です |
| 6〜10層 | あぁ、こういう時ってあんまり気にしないほうが良かったりするんですよ |
| 11〜15層 | そんな日もありますよ…っと。 |
| 16〜20層 | 俺はここまで来れるだけでも凄いって思いますけど |
| 21〜25層 | いやぁ、なんも言えねぇってのが本音です |
| 26〜30層 | どうしてだよぉぉぉお！！！ |
| 31〜35層 | あのぉ…確認なんですけど、30層までって話…どこいったんですかね？ |
| 36〜39層 | あ゛ぁあ゛ぁぁぁあ゛ぁぁあ゛ぁ！！！！ |
| 40層クリア | ちゅっちゅ〜😘 |

- ユーザー指定には「31〜35層」と「32〜39層」があり、32〜35層が重複する。実装上は「31〜35層」を指定どおり優先し、「32〜39層」の意図は重複を避けて「36〜39層」として扱う。
- `MAX_FLOOR = 40` の40層仕様は維持し、40層到達時は40層専用コメントを表示する。
