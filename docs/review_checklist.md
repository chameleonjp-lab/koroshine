# コロシャイン レビュー確認リスト

## 必須成果物

- 確認済み: `index.html` が存在する。
- 確認済み: `README.md` が存在する。
- 確認済み: `docs/specification.md` が存在する。
- 確認済み: `docs/implementation_plan.md` が存在する。
- 確認済み: `docs/review_checklist.md` が存在する。

## スマホ対応

- 確認済み: iPhone SE幅向けの `@media(max-width:340px)` がある。
- 確認済み: 横スクロール抑制の `overflow-x:hidden` がある。
- 確認済み: 長押し選択とテキスト選択を抑制している。
- 確認済み: Canvas上の `touch-action:none` でドラッグ中のページスクロールを抑制している。
- 確認済み: viewport と `gesturestart` でピンチズームを抑制している。
- 要実機確認: iOS Safariでピンチズームが完全に抑制されること。
- 要実機確認: iOS Safariでダブルタップ拡大が邪魔しないこと。
- 確認済み: 入力欄のフォントサイズは16pxである。
- 要実機確認: iPhone SE実機で下部ボタンが押せ、指でスコアや階層が隠れにくいこと。

## 状態管理

- 確認済み: `HOME` 状態がある。
- 確認済み: `RULE` 状態がある。
- 確認済み: `NAME` 状態がある。
- 確認済み: `READY` 状態がある。
- 確認済み: `PLAYING` 状態がある。
- 確認済み: `RESULT` 状態がある。
- 確認済み: `ERROR` 状態がある。
- 確認済み: `addScore` は `PLAYING` 以外で加算しない。
- 確認済み: `update` は `PLAYING` 以外で当たり判定しない。
- 確認済み: `update` は `PLAYING` 以外でゲーム更新しない。
- 確認済み: `requestAnimationFrame` は描画継続するが、状態更新は `PLAYING` のみ。

## ゲーム・スコア

- 確認済み: Canvas中心で描画している。
- 確認済み: 1階突破で +100 される。
- 確認済み: 🌰取得で +50 される。
- 確認済み: 🌰取得時に `🌰 → ☀️` 演出がある。
- 確認済み: 🌰取得時に `+50` が表示される。
- 確認済み: 2連続落下で +50 される。
- 確認済み: 3連続落下で +100 される。
- 確認済み: 4連続落下で +150 される。
- 確認済み: 850ms以内の3連続穴通過で一気落下 +300 される。
- 確認済み: 30階到達で +3000 される。
- 確認済み: 29階から30階へ進んだ時点を30階到達として扱う。
- 確認済み: 危険床接触でゲーム終了する。
- 確認済み: リタイアでゲーム終了し、結果画面に理由を表示する。
- 確認済み: クリア演出がある。
- 確認済み: 危険床接触演出がある。
- 確認済み: 現在階の穴が近い時に視覚補助が出る。
- 確認済み: 通過可能範囲で穴の補助表示がさらに明るくなる。
- 確認済み: 危険床失敗時に約0.35秒、原因位置が見える。
- 確認済み: 失敗待機中に追加スコアやランキング送信を行わない。
- 確認済み: 2連続以上でコンボ表示が出る。
- 確認済み: 一気落下予告が出る。
- 確認済み: 850ms以上空いたら一気落下予告が消える。
- 確認済み: 850ms以上空いたらコンボ表示も消える。
- 確認済み: 失敗待機中は pointerdown / pointermove / リタイアボタン入力を無効化する。
- 確認済み: 結果画面専用に縦スクロールできる。
- 確認済み: CanvasRenderingContext2D.roundRect 未対応ブラウザ向けフォールバックがある。
- 確認済み: 結果画面に到達階や終了理由に応じたコメントが出る。

## ランキング

- 確認済み: `public.scores` を実装で使っていない。
- 確認済み: `public.games` / `public.game_scores` / 既存RPC前提になっている。
- 確認済み: Supabase URL と Publishable key を設定している。
- 確認済み: secret key / service_role key を使っていない。
- 確認済み: `submit_score` へ `p_display_name` / `p_game_slug` / `p_score` / `p_client_version` を送る。
- 確認済み: `get_best_score_ranking` でランキングを取得する。
- 確認済み: ランキング送信はゲーム終了時に自動で行われる。
- 確認済み: ランキング送信は1回のみである。
- 確認済み: ランキング取得は1回のみである。
- 確認済み: 送信失敗時もゲーム結果表示が継続する。
- 確認済み: 送信失敗時に短いユーザー向け文言を表示する。
- 確認済み: 取得失敗時に短いユーザー向け文言を表示する。
- 確認済み: 詳細エラーを画面に長く出さず `console.warn` に留める。
- 確認済み: 結果画面にランキング登録ボタンがない。
- 確認済み: ランキングはベストスコア型、スコア降順である。
- 未確認: Supabase本番RPCの実レスポンス形状との完全一致。

## 性能

- 確認済み: 60fpsを目標に `requestAnimationFrame` を使用している。
- 確認済み: 最低30fpsを意識してdelta timeを制限している。
- 確認済み: 表示床は最大8階である。
- 確認済み: アイテムは最大8個である。
- 確認済み: エフェクトは最大60個である。
- 確認済み: 描画要素は概ね100要素以内である。
- 確認済み: DOM更新はHUD、結果、ランキングなど必要箇所に限定している。
- 確認済み: ランキング通信が送信1回・取得1回である。

## シェア・リンク

- 確認済み: ホームにゲームをシェア導線がある。
- 確認済み: 結果画面に結果をシェア導線がある。
- 確認済み: シェア文にゲーム名、スコア、URLが含まれる。
- 確認済み: Web Share API未対応時のフォールバックがある。
- 確認済み: ホームのその他のゲームURLは `https://chameleonjp.codeberg.page/chameleonjp_lab/` である。
- 確認済み: 結果画面のその他のゲームURLは `https://chameleonjp.codeberg.page/chameleonjp_lab/` である。


## 今回追加したユーザープレイ体験改善の重点確認

- 確認済み: 現在階の穴が近い時の視覚補助を確認する観点を追加した。
- 確認済み: 危険床失敗時に原因が見えることを確認する観点を追加した。
- 確認済み: 2連続以上のコンボ表示を確認する観点を追加した。
- 確認済み: 一気落下予告と `+300` 発生条件維持を確認する観点を追加した。
- 確認済み: 結果コメントを確認する観点を追加した。
- 確認済み: ランキング失敗文言が短く、詳細エラーを画面に長く出さないことを確認する観点を追加した。

## デザイン・操作性・読み込み改善（前回）

- 確認済み: `PLAYING` 遷移時に `resize()` を呼び、Canvas描画解像度を確定する（初回1×1解像度の不具合を修正）。
- 確認済み: `theme-color` を背景上端と一致させ、ホーム追加用メタ（`mobile-web-app-capable` ほか）を追加した。
- 確認済み: 外部ファイル不要のインラインSVGファビコンを追加した。
- 確認済み: `#rankingStatus` に `role="status" aria-live="polite"` を付与した。
- 確認済み: 名前入力でEnterキーから次へ進める。
- 確認済み: `share()` が Web Share / クリップボード / `prompt` の順でフォールバックする。

## 物理演算リニューアル（今回 / 2.0）

iPhone SE相当（375×667・320×568, dpr2, iOS UA）の実ブラウザ自動テストプレイで確認。

- 確認済み: 太陽が重力で落下し、安全床で跳ね返る（`bvy` が正負に振れ `by` が往復、自由落下レンジ約108px）。
- 確認済み: 固定タイムステップ（1/120秒）＋アキュムレータで物理を進め、トンネリングを防ぐ。
- 確認済み: 同心円コイル描画を廃し、遠近楕円リング積層＋中央支柱で塔を表現する。
- 確認済み: 現在階の正面に状態色リング（緑＝穴 / 赤＝危険 / 白＝安全）と階数ラベルを表示する。
- 確認済み: 当たり判定 `front = normAngle(π/2 - 回転角)` が描画の正面と一致する。
- 確認済み: 穴通過で `+100`・コンボ・一気落下 `+300`・🌰→☀️ `+50`・30階 `+3000` が従来通り加点される。
- 確認済み: 危険帯接触で「危険床接触」終了、約0.38秒の失敗表示を行う。
- 確認済み: 実ドラッグで塔が正確に回転（68px≒0.852rad）し、合わせて床を通過できる。
- 確認済み: `ResizeObserver`・`visualViewport`・`orientationchange` 購読とループ内ガードでCanvasが0/1pxにならない。
- 確認済み: `min-height` を `100vh→100dvh` 二段指定、ラッパに `min-height:60vh` を確保した。
- 確認済み: 本番経路（`?debug`無し）でデバッグ用フック `window.__koro` を公開しない。
- 確認済み: 通信はゲーム終了時に送信1回＋取得1回のみ、favicon 404なし、JSエラー0。
- 確認方法: `node --check` 構文OK、iPhone SE自動プレイ（自由落下/反発・穴通過進行・危険帯終了・30階クリア・実ドラッグ通過・本番経路）で全項目パス、`http-server` で 200 応答。

## Helix品質アップデート（今回 / 2.1）

iPhone SE相当（375×667・320×568, dpr2, iOS UA）の実ブラウザ自動テストプレイで確認。

- 確認済み: 中空リング描画を廃し、中実スライス（8分割）＋側面の厚み＋中央支柱で疑似3Dの塔を描画する。
- 確認済み: 床を奥（上）→手前（下）の順で描画し、近い床が遠い床を遮蔽する。
- 確認済み: 3連続落下で太陽がファイアボール化する（`game.fire>0` を観測）。
- 確認済み: ファイアボール中は安全帯・危険帯のスライスを破壊して貫通し、危険帯でも終了しない（正面が危険帯でも死亡せずクリア到達を観測）。
- 確認済み: 通常時に危険帯へ乗ると「危険床接触」で終了する。
- 確認済み: 6階ごとにゾーン配色（安全帯/危険帯/背景）が変化し、背景を連続補間する。
- 確認済み: 太陽は陰影球＋接地影＋スカッシュ＋着地スプラッシュ＋画面シェイクで描画し、回転光線を廃止した。
- 確認済み: スコア式・コンボ・🌰→☀️・30階+3000・状態機械・`submit_score`/`get_best_score_ranking`（送信1/取得1）は不変。スマッシュ加点（+50）を追加。
- 確認済み: 本番経路（`?debug`無し）でデバッグ用フックを公開しない。JSエラー0、favicon 404なし。
- 確認方法: `node --check` 構文OK、iPhone SE自動プレイ（自由落下/反発・30階クリア（ファイア発動）・ファイア中の危険帯貫通・通常時の危険帯終了・本番遷移）で全項目パス、`http-server` で 200 応答。
